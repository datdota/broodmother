{
  "name": "broodmother-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "broodmother — web-based 3D Dota 2 spectator client, driven by the efficient v2 (/ws/stream) entity stream.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "proto": "pbjs -t static-module -w es6 -o src/proto/stream.js -p ../proto entity_frame.proto entity_stream.proto && pbts -o src/proto/stream.d.ts src/proto/stream.js"
  },
  "dependencies": {
    "protobufjs": "^7.4.0",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@gltf-transform/core": "^4.4.2",
    "@gltf-transform/functions": "^4.4.2",
    "@types/node": "^22.20.1",
    "@types/three": "^0.169.0",
    "protobufjs-cli": "^1.1.3",
    "typescript": "^5.6.3",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
